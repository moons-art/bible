rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발/운영 단계: 모든 읽기/쓰기 허용 (추후 보안 규칙 강화 예정)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
